import { UnidadeCervejaPipe } from './unidade-cerveja.pipe';

describe('UnidadeCervejaPipe', () => {
  it('create an instance', () => {
    const pipe = new UnidadeCervejaPipe();
    expect(pipe).toBeTruthy();
  });
});
