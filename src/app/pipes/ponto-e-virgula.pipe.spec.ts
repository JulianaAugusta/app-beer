import { PontoEVirgulaPipe } from './ponto-e-virgula.pipe';

describe('PontoEVirgulaPipe', () => {
  it('create an instance', () => {
    const pipe = new PontoEVirgulaPipe();
    expect(pipe).toBeTruthy();
  });
});
